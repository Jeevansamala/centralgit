pipeline {
    agent none
    stages {
        stage ('Checkout') {
              agent any
                options {
                    skipDefaultCheckout()
                }
            steps {
              echo "Hello World"
            }        
        }
    }
}
